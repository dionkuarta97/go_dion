const initialState = {
    androidVersion: "0.1.45",
    iosVersion: "0.0.0",
};

export function versionReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

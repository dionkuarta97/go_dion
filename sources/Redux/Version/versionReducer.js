const initialState = {
    androidVersion: "1.0.0",
    iosVersion: "1.0.0",
};

export function versionReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
